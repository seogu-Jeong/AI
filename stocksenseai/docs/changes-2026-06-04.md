# 2026-06-04 수정 사항 요약

백엔드 Phase 4-C/4-D 구현 완료 이후 리뷰 피드백 반영 및 실거래 안정성 개선 내역.

---

## Phase 4-C 리뷰 수정 (commit `3d184f9`)

| 항목 | 내용 |
|---|---|
| SIMULATOR_TICKERS 79→80개 | 롯데케미칼(`011170`) 추가 |
| 캐시 커버리지 체크 | `get_prices()` — 5일 허용 오차 기준으로 범위 부족 시 pykrx 재조회 |
| UPSERT | `add_all()` → `ON CONFLICT DO NOTHING` (IntegrityError 방지) |
| download 실패 복원 | 종목별 try/except — 한 종목 실패해도 SSE 스트림 계속 진행 |
| `cash_left_krw` 추가 | lumpsum 응답에 잔여 현금 포함 |
| recurring 응답 개선 | `start_date_actual`, `end_date_actual` 포함, 최종 평가 포인트 chart_data 추가 |
| 미래 날짜 차단 | `sell_date`, `end_date` 미래 입력 → 422 |

---

## Phase 4-D 구현 (commits `0e50ce4`, `ee30310`, `6765ed8`)

| 항목 | 파일 | 내용 |
|---|---|---|
| 관심종목 CRUD | `backend/api/routes/watchlist.py` | 그룹/아이템 8개 엔드포인트 (TDD, 6 테스트) |
| 목표가 알림 | `backend/tasks/email_tasks.py` | `check_price_alerts` — Redis 24h 쿨다운, watchlist_price 설정 연동 |
| 일일 손실 자동 차단 | `backend/tasks/email_tasks.py` | `check_daily_loss` — real 모드만 trading_blocked 설정 |
| 이메일 태스크 | `backend/tasks/email_tasks.py` | `send_price_alert_email` Celery 태스크 추가 |

---

## Phase 4-D 리뷰 수정 (commit `f42ceb2`)

| 항목 | 내용 |
|---|---|
| mode 분리 | Redis 쿨다운 키에 mode 포함 (`daily_loss_alert:{uid}:{mode}`) — paper 손실이 real 차단 안 함 |
| KST 날짜 | `_check_daily_loss_async` — `zoneinfo.ZoneInfo("Asia/Seoul")` 기준 자정으로 수정 |
| 목표가 제거 지원 | `update_item` — `model_fields_set` 사용, `null` 전송 시 목표가 삭제 가능 |
| IntegrityError → 409 | `add_item` — commit 시 UniqueConstraint 충돌 → rollback + 409 |
| ImportError guard 제거 | 모듈 레벨 import, 실패 시 명시적 에러 발생 |
| 가격 조회 실패 로그 | `_logger.warning()`으로 기록 |

---

## 실거래 포트폴리오 KIS API 전환 (commits `9de8fb8`, `089103d`)

| 항목 | 파일 | 내용 |
|---|---|---|
| `get_balance_full()` 추가 | `backend/services/kis_service.py` | KIS `inquire-balance` output1(종목별) + output2(요약) 파싱 |
| real 모드 포트폴리오 교체 | `backend/api/routes/portfolio.py` | `user.mode == "real"` → KIS API, KIS 키 미설정 시 DB fallback |
| 계좌번호 하이픈 처리 | `backend/services/kis_service.py` | `_get_keys()` — `.replace("-", "")` 일괄 처리 (5군데 split 안전) |

---

## 백엔드 안정성 개선 (commit `6877c89`)

| # | 항목 | 파일 | 내용 |
|---|---|---|---|
| 3 | 시장가 리스크 계산 | `backend/api/routes/trades.py` | MARKET 주문 → 현재가 조회 후 리스크 계산, 조회 실패 시 400 차단 |
| 5 | 폴링 실패 상태 | `backend/tasks/order_tasks.py` | 최대 재시도 초과 → `PENDING` → `UNKNOWN` 상태로 표시 |
| 10 | 운영 환경 보안 기본값 | `backend/core/config.py` | `APP_ENV` ∉ {development, test} 시 기본 SECRET_KEY/ENCRYPTION_KEY 사용 차단 |
| 11 | PriceCache 마이그레이션 등록 | `db/migrations/env.py` | `from models.price_cache import PriceCache` 추가 |
| 12 | README 경로 수정 | `README.md` | `.env.example` 경로, alembic 실행 위치, bcrypt 버전 주의사항 추가 |
| - | progress.md AI 표현 | `docs/progress.md` | Phase 3 — LSTM 가중치 미포함 명시 |

---

## 실현손익 Ledger (commit `d863cdb`)

| 항목 | 파일 | 내용 |
|---|---|---|
| 마이그레이션 | `db/migrations/versions/b8c9d0e1f2a3_...py` | `trades` 테이블에 `realized_pnl INTEGER` 컬럼 추가 |
| Trade 모델 | `backend/models/trade.py` | `realized_pnl` 컬럼 추가 |
| 실현손익 계산 | `backend/tasks/order_tasks.py` | SELL 체결 시 `(executed_price − Portfolio.avg_price) × quantity` 저장 |
| 일일 손실 | `backend/services/risk_service.py` | `_get_today_loss()` — `realized_pnl` 사용 (이전 레코드는 order_price fallback) |
| 포트폴리오 성과 | `backend/api/routes/portfolio.py` | `get_performance()` / `get_metrics()` — `realized_pnl` 기반으로 교체 |

**실현손익 계산 근거:**
- `Portfolio.avg_price` = BUY 체결 시 가중평균으로 업데이트되는 정확한 취득단가
- 시장가 매도 포함, 부분 체결 정확 반영
- 기존 `executed_price − order_price` (매도 주문가 기준) 문제 해결

---

## 미수정 항목 (별도 Phase 필요)

| 항목 | 이유 |
|---|---|
| LSTM 학습 가중치 | 실제 학습 데이터·컴퓨팅 필요 |
| 기술적 지표 점수 정책 | 전략 설계 문제 |
| WebSocket 재연결/복원 | 운영 인프라 고도화 |
| 백테스트 Celery 비동기 | 아키텍처 변경 |
| 미체결/정정/취소 reconciler | 신규 서비스 레이어 필요 |
